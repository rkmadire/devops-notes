Multiple ways to build image 
-------------------------------
Build Docker image from github repository
	
	$ docker build https://github.com/thej950/project-1#main -f folder-1/Dockerfile

Build an image according to specified dockerfile 
	
	$ docker build -f build/DockerFile -t my-image:tag .

Build an image with customised tah name
	
	$ docker build -t myimage:1.0.1 .

To build image 
	
	$ docker build .  

	